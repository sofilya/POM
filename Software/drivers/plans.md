# Plans for drivers directory

## This directory is probably just temporary and will be gone in some time as we soon find out the drivers HAVE (/should) be in the modules or kernel/drivers directory instead.

- the bt.c *(temporary name)* will be mostly a copy of some specific driver with few minor changes to work with our own hardware
